const API='http://localhost:3000';
async function load(){try{const p=await fetch(API+'/api/plans').then(r=>r.json());document.getElementById('plans').innerHTML=p.map(x=>`<div class="plan"><h3>${x.name}</h3><p>💾 ${x.ram_mb} MB RAM</p><p>🗄️ ${Math.round(x.disk_mb/1024*10)/10} GB disco</p><p>🤖 ${x.max_apps} aplicação(ões)</p><button>Escolher plano</button></div>`).join('');const a=await fetch(API+'/api/apps').then(r=>r.json());document.getElementById('apps').textContent=a.length}catch(e){console.log(e)}}function login(){alert('Configure o Discord OAuth2 no backend para ativar o login.')}load();